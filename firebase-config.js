/* RepForge cloud configuration.
   Firebase web configuration is public by design. Replace the null object with
   your Firebase project's web-app configuration to enable account + sync.
   See SETUP-SYNC.md. */
window.REPFORGE_FIREBASE_CONFIG = null;
